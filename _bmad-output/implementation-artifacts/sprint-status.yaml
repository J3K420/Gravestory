# generated: 2026-06-05
# last_updated: 2026-06-08-session14b
# project: GraveStory
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic 1 is in-progress: Phase 9 is actively being worked on
# - Epic 1 stories have a soft dependency chain for RevenueCat:
#   1.1 (DB migration) → 1.3 (webhook) → 1.4 (SDK re-enable)
#   Stories 1.2, 1.5, 1.6 are independent and can be parallelised
# - Epic 2 blocked until ALL Epic 1 stories are done
# - Epic 3 stories are fully independent of each other; sequence by priority post-launch
# - Developer typically creates next story file after previous one is 'done'

generated: 2026-06-05
last_updated: 2026-06-08-session14b
project: GraveStory
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ─── Epic 1: Phase 9 Completion ───────────────────────────────────────────
  epic-1: in-progress

  # RevenueCat dependency chain: 1.1 must precede 1.3, 1.3 must precede 1.4
  1-1-run-scan-credits-database-migration: done
  1-2-add-cloudflare-worker-origin-validation: done
  1-3-build-revenuecat-purchase-webhook-endpoint: done
  1-4-re-enable-revenuecat-sdk-in-mobile-app: done  # Session 14, 2026-06-08, commit 1ef17ea. Real goog_ Android key obtained from RevenueCat dashboard, stored as EAS Secret REVENUECAT_API_KEY (project scope, STRING; confirmed present in `production` env via eas env:list). app.config.js exposes extra.revenueCatApiKey from process.env; src/lib/config.js reads it via expo-constants with the test key as LOCAL-DEV-ONLY fallback (test key can no longer reach a Play build -- the original crash cause from revert 0634269). Restored Purchases.configure (DEBUG log gated on __DEV__) + paywall getOfferings/purchasePackage/restorePurchases. Added expo-constants ~18.0.13. versionCode bumped 3->4 (8ecd51c). NOTE: requires a fresh NATIVE build to ship (RC is native, OTA won't carry it). Pre-flight for live purchases: confirm RC dashboard has a current/default offering published + Play Console in-app products gravestory_5/20/60_scans created & linked.
  1-5-publish-privacy-policy-page: done
  1-6-create-play-store-listing-assets: in-progress  # text+SVG done; screenshots pending device transfer

  epic-1-retrospective: optional

  # ─── Epic 2: Play Store Launch ────────────────────────────────────────────
  # Play Store account activated 2026-06-06. Unblocked pending Story 1.4 RC key.
  epic-2: in-progress

  2-1-generate-android-production-credentials: done  # keystore confirmed existing; downloaded backup 2026-06-07
  2-2-build-and-submit-production-aab-to-play-store: in-progress  # AAB in Play Console internal testing; tester j3k420@gmail.com added. ✅ WHITE-MAPS BLOCKER RESOLVED (Session 12, 2026-06-08): maps rendered solid white on the production AAB (internal track). Native issue, NOT OTA-fixable. Root cause: Google Play App Signing re-signs the AAB, so on-device cert != EAS keystore; Maps Android API key was restricted to the EAS SHA-1 only, so Google rejected the Play-signed install -> blank tiles. FIXED by adding the Play app-signing SHA-1 (E0:DB:1A:DC:E2:39...) alongside the existing EAS SHA-1 (97:CA:93:16...) on the "Maps Platform API Key" in GCP project GraveHunter (APIs & Services -> Credentials -> Application restrictions -> Android apps, package com.gravestory.app). Verified working on device. Navigation note for next time: App signing lives under Protected with Play (shield icon) -> Play Store protection card -> expand -> Play app signing -> "App signing key certificate" SHA-1 (NOT under Test and release; "App integrity" not present on this account). SEPARATE finding: EAS GOOGLE_MAPS_ANDROID_API_KEY secret exists ONLY in the `production` environment; `preview`/`development` envs have NO vars, so preview/dev APK builds compile an empty key and also show white maps (key is in local mobile/.env; eas env:create to fix). Session 11: mobile primary Gemini model bumped gemini-2.5-flash-lite -> gemini-3.1-flash-lite (web parity); committed 539ec8e, OTA'd (group 43c05b1b). Session 12: biography formatting fix -- validateCitations strips non-numeric [Word] bracket labels (e.g. [Inscription]/[Notes] that were leaking into prose); biography prompt FORMATTING section forces \n\n paragraph breaks (one+ per person on multi-subject stones) + bans label-style brackets; web+mobile parity; commits 06b0ab1 + 44b41ac, OTA group 142aac57 to production. Gemini 3.1 confirmed actually running on mobile (proxy response modelVersion echoes gemini-3.1-flash-lite, not a 404 fallback). NEXT (in order): (1) optional bio-prompt tuning (no longer blocking); (2) build new production AAB (npx eas build --platform android --profile production) -- RC native module + current AAB lacks RC SDK, rebuild required; (3) upload to Play Console; (4) recruit 12 testers -> 14-day closed-testing clock. Tester unlimited-credits flow mapped: emails -> Play Console list -> testers sign in -> batch is_unlimited SQL keyed on email -> token-refresh latency (~1hr/restart; onboarding note promises up to 48hr). Session 10 infra audit (still valid): Gemini on paid Tier 1 (key under AI Studio "Gemini Project" gen-lang-client-0734604382); all 6 Supabase migrations live (006 fn is add_scan_credits); RC Pub/Sub warning ignorable (credits-only). Session 13 (2026-06-08): UX polish -- mobile CemeteryMapScreen grave-marker callout now shows a drag-to-correct hint ("Press and hold the marker, then drag it to the correct grave"). Markers were already draggable (onDragEnd -> save-correction Alert -> userCorrected:true) but nothing surfaced the affordance. New calloutHint style (flame, italic, 11px). JS-only; commit fb47e33, OTA group abba15da to production. Session 14 (2026-06-08): RC SDK re-enabled (Story 1.4 now done) -> kicked off the first RC-enabled production AAB (versionCode 4). First build f62e7ff6 ERRORED after 21s in the Install dependencies phase -- expo-constants was added to package.json as a direct dep but only existed transitively in package-lock.json, so EAS `npm ci` failed the sync check. FIXED by running a full `npm install` to hoist expo-constants to the top level (matched expo's resolved ~18.0.13) + regenerate lockfile; `npm ci --dry-run` passes; committed edb132c. Re-launched build d2810dc4 (RC-enabled, versionCode 4) -- FINISHED successfully; AAB at https://expo.dev/artifacts/eas/rZDCJDtZkuSnJwR3ZZP8iV.aab. Lesson: when adding a direct dep to a mobile dep that was previously only transitive, must run a real npm install (not --package-lock-only) so npm writes the top-level node_modules/<pkg> lockfile entry, else EAS npm ci fails fast. ---- Session 14 cont'd: versionCode 4 AAB UPLOADED + RELEASED to Internal testing (release "3"). Gotcha: bundle was already in the library, so re-uploading gave "Version code 4 has already been used" -- correct path is "Add from library" not Upload. To find a versionCode 4 release in the releases list it must first be attached to a release (a bundle sitting only in the library is invisible there). Internal-testing TESTERS added (j3k420@gmail.com + edmondsj46@gmail.com) via store-listing/testers.csv (header-less, one email per line -- a header row "email" is rejected as an invalid email). LICENSE TESTING configured: Settings (org-level, NOT in-app) -> Monetization -> License testing -> Email lists -> list "testerss" (2 users) checked -> License response RESPOND_NORMALLY -> Save. License testers != internal testers (separate lists; both required: install access + free test card). PURCHASE-TEST BLOCKER: j3k420@gmail.com is is_unlimited in Supabase -> paywall never appears for it; must test with a non-unlimited account (edmondsj46@gmail.com -- unverified whether it's also unlimited). To buy without charge: same Gmail signed into BOTH phone Play Store AND the app, account on license-tester list, paywall shows real Google prices (pkg.product.priceString) = RC->Google chain proven. PRE-FLIGHT for paywall to populate: RC dashboard must have a current/default Offering published with the 3 products as packages (paywall reads offerings.current); Play Console in-app products gravestory_5/20/60_scans created + Active + linked in RC (products often need a few hours to propagate after first upload). NEXT: complete RC offering/products setup -> test purchase with non-unlimited license-tester account -> verify webhook (Story 1.3) grants credits in Supabase scan_credits.

  epic-2-retrospective: optional

  # ─── Epic 3: Post-Launch Enhancements ────────────────────────────────────
  # All stories independent; sequence by business priority after public launch
  epic-3: backlog

  3-1-familysearch-genealogy-integration: backlog
  3-2-web-payment-flow: backlog
  3-3-ios-app-store-launch: backlog
  3-4-biography-accuracy-feedback-mechanism: backlog
  3-5-accessibility-audit-and-remediation: backlog

  epic-3-retrospective: optional
