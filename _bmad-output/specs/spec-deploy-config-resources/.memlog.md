---
topic: Deploy-specific public configuration and attached backing-service handles
updated: 2026-07-14T21:05
---

- (capability) CAP-1: Each deployable component has one documented source for deploy-varying public resource handles and one contract for secret injection.
- (capability) CAP-2: Validation detects missing, placeholder, or accidentally permissive configuration before a release is eligible.
- (capability) CAP-3: Backing-service locators can change between deploys without editing unrelated application logic.
- (constraint) Public client identifiers are not misrepresented as secrets; true service credentials remain server-side.
- (constraint) Any change to EAS or Cloudflare remote environment state requires explicit production approval and is not performed by this batch.
- (constraint) Mobile changes must follow the repository-required exact Expo documentation before implementation.
- (non-goal) No secret rotation, provider migration, web bundler, or production environment mutation.
- (event) Coherence validation passed.
- (event) Preservation validation passed against the program audit and platform-specific configuration constraints.
- (event) Spec finalized after coherence and preservation validation.
- (decision) The configuration contract covers the metrics digest alongside web, mobile, and Worker units.
- (decision) Optional and feature-gated handles use explicit conditional-required rules rather than blanket presence checks.
- (capability) CAP-4: Endpoint replacement preserves compatibility for installed clients until a minimum supported version and approved retirement gate are satisfied.
- (c) o
- (D) e
- (event) The immediately preceding entries tagged c and D were malformed tool-invocation artifacts with no semantic effect.
- (constraint) Depends on deterministic verification plus Worker and database configuration inventories.
- (decision) Local validation proves supplied configuration shape but remote secret or binding presence remains an attestation or approval-bound read-only check.
- (decision) Endpoint retirement retains every locator needed by supported installed generations and requires adoption or enforced-version evidence plus owner approval.
- (event) Final BMad Blind Hunter, Edge Case Hunter, and Acceptance Auditor re-review passed after triage.
