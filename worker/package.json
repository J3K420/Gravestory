{
  "name": "gravestory-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.13.1"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "test": "node --test tests/config.test.mjs tests/runtime.test.mjs",
    "verify": "node --check worker.js && node --check config.js && node --check runtime.js && npm test && wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
